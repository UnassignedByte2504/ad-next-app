# =============================================================================
# 📦 NPM Registry Configuration
# =============================================================================
# Configures npm/bun to use the private Verdaccio registry.
#
# USAGE:
#   1. Start Verdaccio: docker compose -f infrastructure/registry/docker-compose.registry.yml up -d
#   2. This file will automatically be used by npm/bun
#
# For CI/CD, set NPM_TOKEN environment variable.
# =============================================================================

# Use private registry (comment out if not running Verdaccio)
# registry=http://localhost:4873/

# Fallback to npmjs.org (default)
registry=https://registry.npmjs.org/

# Scoped packages - always use official registries
@types:registry=https://registry.npmjs.org/
@emotion:registry=https://registry.npmjs.org/
@mui:registry=https://registry.npmjs.org/
@sentry:registry=https://registry.npmjs.org/
@storybook:registry=https://registry.npmjs.org/
@testing-library:registry=https://registry.npmjs.org/
@playwright:registry=https://registry.npmjs.org/
@vitejs:registry=https://registry.npmjs.org/
@vitest:registry=https://registry.npmjs.org/
@tailwindcss:registry=https://registry.npmjs.org/
@floating-ui:registry=https://registry.npmjs.org/

# Private packages (when Verdaccio is running)
# @ayla-designs:registry=http://localhost:4873/

# Authentication for private registry (CI/CD)
# //localhost:4873/:_authToken=${NPM_TOKEN}

# Strict SSL (disable for local dev with self-signed certs)
strict-ssl=true

# Engine strict - fail on incompatible node version
engine-strict=true

# Save exact versions
save-exact=true

# Audit level
audit-level=high

# Progress bar
progress=true

# Fund messages
fund=false
