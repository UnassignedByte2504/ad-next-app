# Architecture Diagrams

> Visualización de la arquitectura de Server Components.

## Browser-Server Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     BROWSER (Client)                        │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  HTML from Server (already rendered)                  │ │
│  │  ┌──────────────────────────────────────────────┐    │ │
│  │  │  <html>                                       │    │ │
│  │  │    <body>                                     │    │ │
│  │  │      <header>Bemyre</header>    ← Server     │    │ │
│  │  │      <main>                                   │    │ │
│  │  │        <article>Musicians</article> ← Server │    │ │
│  │  │        <div id="search-bar">    ← Client JS  │    │ │
│  │  │        <div>Results...</div>    ← Server     │    │ │
│  │  │        <button id="load-more">  ← Client JS  │    │ │
│  │  │      </main>                                  │    │ │
│  │  │    </body>                                    │    │ │
│  │  │  </html>                                      │    │ │
│  │  └──────────────────────────────────────────────┘    │ │
│  │                                                       │ │
│  │  Solo Client Components envían JS:                   │ │
│  │  ✓ providers.tsx (~50 KB - MUI + Theme)             │ │
│  │  ✓ SearchBar.tsx (~5 KB)                            │ │
│  │  ✓ LoadMoreButton.tsx (~2 KB)                       │ │
│  │                                                       │ │
│  │  Total JS: ~57 KB                                    │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                           ▲
                           │ HTML Response
                           │
┌──────────────────────────┴──────────────────────────────────┐
│                     SERVER (Next.js)                        │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  app/musicians/page.tsx (Server Component)           │ │
│  │                                                       │ │
│  │  export default async function MusiciansPage() {     │ │
│  │    const musicians = await fetchMusicians();         │ │
│  │    return (                                           │ │
│  │      <Container>              ← Server               │ │
│  │        <h1>Musicians</h1>     ← Server               │ │
│  │        <SearchBar />          ← Client (boundary)    │ │
│  │        <MusicianList />       ← Server               │ │
│  │      </Container>                                    │ │
│  │    );                                                │ │
│  │  }                                                   │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## Component Tree

```
app/
└── musicians/
    └── page.tsx (Server Component)
        ├── layout.tsx (Server Component)
        │   └── Providers (Client Component) ← Client Boundary
        │       └── ThemeProvider (Client)
        │           └── ErrorBoundary (Client)
        │               └── {children} ← Back to Server!
        │
        └── Page Content (Server Components)
            ├── <Container> (MUI - works in Server)
            ├── <Typography> (MUI - works in Server)
            ├── <SearchBar /> (Client Component) ← "use client"
            │   ├── useState for query
            │   └── onChange handlers
            ├── <MusicianGrid> (Server Component)
            │   └── musicians.map(m => (
            │       <MusicianCard> (Server Component)
            │           ├── <Card> (MUI - Server)
            │           ├── <Typography> (MUI - Server)
            │           └── <FollowButton> (Client) ← "use client"
            │               └── onClick + useState
            └── <Pagination> (MUI - Server)
```

## Data Flow

```
┌─────────────────┐
│  User Request   │
│  /musicians     │
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│  Server Component       │
│  (page.tsx)             │
│                         │
│  async function() {     │
│    const data =         │
│      await fetchData(); │◄───┐
│  }                      │    │
└────────┬────────────────┘    │
         │                     │
         ▼                     │
┌─────────────────────────┐    │
│  Render Tree            │    │
│                         │    │
│  Server Components:     │    │
│  - Render to HTML       │    │
│  - No JS bundle         │    │
│                         │    │
│  Client Components:     │    │
│  - Placeholder in HTML  │    │
│  - JS bundle created    │    │
└────────┬────────────────┘    │
         │                     │
         ▼                     │
┌─────────────────────────┐    │
│  HTML Response          │    │
│  + Client JS bundles    │    │
└────────┬────────────────┘    │
         │                     │
         ▼                     │
┌─────────────────────────┐    │
│  Browser                │    │
│  - Render HTML          │    │
│  - Download JS          │    │
│  - Hydrate Client       │    │
│    Components           │    │
└─────────────────────────┘    │
                               │
         Backend API ──────────┘
         /api/v1/musicians
```

## Bundle Size Comparison

### Optimal (Implementación Actual)

```
┌──────────────────────────────────────┐
│  Page Load                           │
├──────────────────────────────────────┤
│  HTML: ~50 KB (pre-rendered)         │
│  CSS: ~20 KB (MUI + Tailwind)        │
│  JS:                                 │
│    - providers.tsx: 50 KB            │
│    - SearchBar.tsx: 5 KB             │
│    - FollowButton.tsx: 2 KB          │
│  ──────────────────────────────────  │
│  Total JS: ~57 KB                    │
│  Total: ~127 KB                      │
└──────────────────────────────────────┘
```

### Si Todo Fuera Client-Side

```
┌──────────────────────────────────────┐
│  Page Load                           │
├──────────────────────────────────────┤
│  HTML: ~5 KB (empty shell)           │
│  CSS: ~20 KB (MUI + Tailwind)        │
│  JS:                                 │
│    - React Runtime: 50 KB            │
│    - Next.js Runtime: 70 KB          │
│    - providers.tsx: 50 KB            │
│    - page.tsx: 20 KB                 │
│    - All components: 30 KB           │
│  ──────────────────────────────────  │
│  Total JS: ~220 KB                   │
│  Total: ~245 KB                      │
│                                      │
│  💾 Ahorro con RSC: ~118 KB (-48%)  │
└──────────────────────────────────────┘
```

## Rendering Timeline

### Traditional Client-Side React

```
┌────────────────────────────────────────────────────────────┐
│ Time (ms)                                                  │
├────────────────────────────────────────────────────────────┤
│    0 │ Request sent                                        │
│  200 │ HTML received (empty shell)                         │
│  250 │ JS download starts                                  │
│  800 │ JS download complete                                │
│ 1000 │ JS parsing & execution                              │
│ 1500 │ React rendering                                     │
│ 1800 │ Data fetching starts (useEffect)                    │
│ 2200 │ Data received                                       │
│ 2400 │ Re-render with data                                 │
│ 2500 │ ✓ First meaningful paint                           │
└────────────────────────────────────────────────────────────┘
```

### With Server Components

```
┌────────────────────────────────────────────────────────────┐
│ Time (ms)                                                  │
├────────────────────────────────────────────────────────────┤
│    0 │ Request sent                                        │
│  100 │ Server fetches data (parallel)                      │
│  200 │ Server renders HTML                                 │
│  300 │ HTML received (with content!)                       │
│  350 │ ✓ First meaningful paint                           │
│  400 │ Client JS download starts (smaller)                 │
│  600 │ Client JS execution                                 │
│  700 │ Hydration of interactive parts                      │
│  750 │ ✓ Interactive                                      │
└────────────────────────────────────────────────────────────┘

📊 Mejora:
  - First Paint: 2500ms → 350ms (-86%)
  - Time to Interactive: 2500ms → 750ms (-70%)
```

## Decision Tree (Visual)

```
                    ┌─────────────────┐
                    │  New Component  │
                    └────────┬────────┘
                             │
                   ┌─────────▼─────────┐
                   │ Uses hooks?       │
                   │ (useState, etc)   │
                   └─────┬─────┬───────┘
                         │     │
                    YES  │     │  NO
                         │     │
          ┌──────────────┘     └──────────────┐
          │                                   │
┌─────────▼─────────┐              ┌─────────▼─────────┐
│ "use client"      │              │ Uses event        │
│ Required          │              │ handlers?         │
└───────────────────┘              └─────┬─────┬───────┘
                                         │     │
                                    YES  │     │  NO
                                         │     │
                          ┌──────────────┘     └──────────────┐
                          │                                   │
                ┌─────────▼─────────┐              ┌─────────▼─────────┐
                │ Can extract to    │              │ Uses browser      │
                │ smaller client    │              │ APIs?             │
                │ component?        │              └─────┬─────┬───────┘
                └─────┬─────┬───────┘                    │     │
                      │     │                       YES  │     │  NO
                 YES  │     │  NO                        │     │
                      │     │                            │     │
           ┌──────────┘     └──────────────┐             │     │
           │                               │             │     │
┌──────────▼──────────┐        ┌──────────▼──────────┐   │     │
│ Extract to small    │        │ "use client"        │   │     │
│ Client Component    │        │ Required            │   │     │
│ (Optimal!)          │        └─────────────────────┘   │     │
└─────────────────────┘                                  │     │
                                         ┌───────────────┘     │
                                         │                     │
                               ┌─────────▼─────────┐   ┌───────▼───────┐
                               │ "use client"      │   │ Server        │
                               │ Required          │   │ Component     │
                               └───────────────────┘   │ (Optimal!)    │
                                                       └───────────────┘
```

## Related

- [Architecture Overview](./overview.md)
- [Common Patterns](../patterns/common-patterns.md)
- [Performance](../best-practices/performance.md)

